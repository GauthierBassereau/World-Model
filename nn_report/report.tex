\documentclass[10pt,a4paper]{article}

\usepackage[margin=2.2cm]{geometry}
\usepackage{times}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{array}
\usepackage{float}
\usepackage[colorlinks=true,linkcolor=black,citecolor=black,urlcolor=blue]{hyperref}

\setlength{\parindent}{0pt}
\setlength{\parskip}{0.55em}
\setlength{\emergencystretch}{2em}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}

\newcommand{\resultfigure}[2]{%
  \IfFileExists{#2}{%
    \includegraphics[width=#1]{#2}%
  }{%
    \fbox{\parbox[c][0.14\textheight][c]{0.92\linewidth}{%
      \centering Figure placeholder\\[1mm]
      \texttt{\detokenize{#2}}%
    }}%
  }%
}

\title{\vspace{-1.5cm}\textbf{Patch-Level Diffusion Forcing for a Semantic Robot World Model}}
\author{Gauthier Bassereau\\
\small Neural Networks Project, University of Tartu}
\date{June 2026}

\begin{document}
\maketitle
\vspace{-0.8cm}

\section{Project Idea and Scope}

World models learn to predict how an environment changes over time. For robot manipulation, this is difficult because an image contains regions with very different levels of predictability. Static background patches are usually easy, while robot grippers, small objects, contact boundaries, and occlusions are much harder. Nevertheless, conventional diffusion and flow-matching models assign one noise level to the entire image and spend the same denoising effort everywhere.

I ask a narrow question: \emph{does training a robot world model with different noise levels across the patches of the same image improve its spatial and temporal understanding?} My motivation comes from Patch Forcing \cite{schusterbauer2026patch}, which extends Diffusion Forcing \cite{chen2024diffusionforcing} from sequence elements to image patches. Its main idea is that partially resolved regions can provide context for more difficult regions. I find this particularly relevant to video dynamics: a clear view of the table, robot arm, or part of an object may help the model reconstruct a nearby patch affected by motion or occlusion.

I build on the world-model code developed for my thesis, but I keep the course contribution separate and clearly scoped. For this project, I:
\begin{itemize}
    \item converted the BridgeData V2 experiments to a simple episode-based LeRobot evaluation setup;
    \item implemented patch-wise noise sampling while preserving the original frame-level baseline;
    \item tested two alternatives for signal conditioning;
    \item added a patch-difficulty prediction head and an adaptive sampler for later experiments; and
    \item compared frame-level and patch-level training quantitatively and through decoded video rollouts.
\end{itemize}

\section{Starting Model}

I encode each RGB frame with a frozen DINOv2-base encoder \cite{oquab2023dinov2}. The resulting state is a $16\times16$ grid of 256 patch tokens, each with 768 features. I use a 24-block spatial-temporal transformer with width 1024 and 16 attention heads to predict future DINO features. My model has approximately 400 million parameters. Spatial attention connects patches within a frame, while causal temporal attention carries information between frames. I supply robot actions through a separate action token.

I train the model with a linear flow-matching path. Given a clean latent patch $z$, Gaussian noise $\epsilon$, and signal level $s\in[0,1]$, I construct the corrupted input as
\begin{equation}
    z_s = s z + (1-s)\epsilon ,
\end{equation}
where $s=0$ is pure noise and $s=1$ is clean data. I train the network with a velocity objective. In my original setup, each frame $t$ has one sampled signal level $s_t$, shared by all 256 patches. This is already Diffusion Forcing across time because different frames in a sequence can have different signal levels, but the corruption remains uniform within each image.

\section{Patch Forcing Extension}

\subsection{Patch-level corruption}

For Patch Forcing, I first sample a frame-level base signal $b_t$ from the same configurable scheduler used by the baseline. In these experiments the base distribution is logit-normal. Each patch signal is then sampled from the lower half of a Gaussian centered on $b_t$:
\begin{equation}
    s_{t,p} \leq b_t, \qquad
    s_{t,p} \sim \mathcal{N}\!\left(b_t,\,
    \min\left(\frac{b_t}{2},\,0.6\right)^2\right)
    \text{ truncated below } b_t .
\end{equation}
Values falling below zero are resampled inside the valid interval. The base signal therefore controls the maximum amount of clean information in the frame, while the patches span a range of harder, noisier states. This follows the Logit-Normal Truncated Gaussian motivation of Schusterbauer et al.\ \cite{schusterbauer2026patch}: unconstrained independent patch timesteps can leak nearly clean context into almost every training example, whereas truncation keeps the patch distribution tied to a meaningful global state.

\subsection{Conditioning ablation}

My main design question was how to tell the transformer which noise level belongs to each patch. The source paper uses spatially varying timestep conditioning through adaptive normalization. My architecture does not use AdaLN-Zero, so I first concatenated a learned 256-dimensional signal embedding to every 768-dimensional DINO token before projecting it into the transformer. This gave the network explicit access to every $s_{t,p}$, but it also changed the visual input interface and attached a large learned code to every patch.

I did not obtain reliable training with this version. Evaluation initially improved, then rose sharply at approximately 12,000 steps and did not recover. My current interpretation is that concatenating the learned signal embedding interfered with the structure of the pretrained DINO representation. I cannot establish this from a single run, however, and this conditioning mechanism requires further investigation.

In my final version, I remove per-patch signal conditioning entirely. I still corrupt every patch with its individual signal level, but I give the model only the original frame-level signal token containing $b_t$. The model must infer which regions are trustworthy from the noisy features and from their spatial and temporal context.

My main goal is to improve the learning signal, rather than primarily to accelerate generation by denoising easy patches before difficult ones. Across training, the same image can appear with many different spatial corruption patterns. Since I do not reveal the local signal levels, the transformer must compare neighboring regions, recover object boundaries and identities, and use clearer patches to interpret noisier ones. I expect this to encourage the model to learn stronger spatial relationships and more persistent object representations instead of solving every patch as an isolated denoising problem.

I also implemented the difficulty head and adaptive sampler proposed in the source paper. I train the head to predict one log-variance value per patch from the detached velocity error, and I use this score to let the sampler advance easy patches while refining difficult patches with smaller inner steps. This was not the main focus of my experiments: I implemented and tested the mechanism, but my primary question was whether patch-wise corruption itself provides a better training objective for a world model.

\section{Data and Experimental Setup}

I use BridgeData V2 \cite{walke2023bridgedatav2} in LeRobot format. Bridge episodes are short, typically around seven seconds, so sampling many starting indices from the same episode would produce highly overlapping examples. I therefore use one dataset index per episode, always beginning at the episode start. I extract 40 frames at 5 Hz for training, covering most or all of a typical trajectory. This also reduces the amount of terminal padding that complicated my earlier experiments.

I reserve 32 randomly selected episodes for evaluation and exclude them from training. I use the same split and evaluation procedure for every model. I report two complementary metrics:
\begin{itemize}
    \item \textbf{Teacher-forced prediction} measures one-step generation. For every valid adjacent frame pair, I give the model a clean ground-truth frame as context and ask it to generate the next frame using the corresponding action. I then compute mean squared error in DINO feature space. Since generated predictions are not fed back into the model, this metric mainly measures local denoising and next-state prediction.
    \item \textbf{Autoregressive rollout} measures error accumulation. I provide ten clean context frames, then generate the future sequence one frame at a time using the ground-truth actions. Every generated latent is fed back into the temporal context before generating the next frame. The horizon-$H$ metric averages DINO-feature MSE over the first $H$ generated frames; for example, horizon 20 summarizes the first 20 rollout steps rather than only the twentieth frame.
\end{itemize}
I also decode selected DINO rollouts to RGB for qualitative inspection, but I do not use decoded pixels as a training or evaluation loss.

\begin{table}[H]
\centering
\small
\begin{tabular}{L{4.0cm}L{9.5cm}}
\toprule
\textbf{Component} & \textbf{Configuration} \\
\midrule
Training data & BridgeData V2, one sample per episode, 40 frames at 5 Hz \\
Evaluation data & 32 held-out episodes, fixed across experiments \\
Visual representation & Frozen DINOv2-base, 256 patches $\times$ 768 features \\
World model & 24 transformer blocks, width 1024, 16 heads, approximately 400M parameters \\
Optimization & AdamW, bfloat16 distributed training, velocity loss \\
Compute & Six NVIDIA H200 GPUs; approximately two days per full run \\
Main metric & Held-out DINO-feature MSE for teacher-forced and autoregressive prediction \\
\bottomrule
\end{tabular}
\caption{Experimental setup shared by the baseline and successful Patch Forcing run.}
\label{tab:setup}
\end{table}

\section{Experiments and Results}

\subsection{Failed explicit patch conditioning}

In my first Patch Forcing run, I used the concatenated per-patch signal embedding. Figure~\ref{fig:failed-conditioning} should show both its training curve and held-out evaluation curve. The important observation is not a single noisy evaluation point but the sustained degradation beginning around 12,000 steps. The training loss did not provide an adequate warning, which reinforced the need for autoregressive held-out evaluation.

\begin{figure}[H]
    \centering
    \resultfigure{0.92\linewidth}{images/failed_patch_conditioning_curves.pdf}
    \caption{Training and evaluation curves for the failed variant with explicit learned signal embeddings concatenated to every DINO patch token. Evaluation performance deteriorated after approximately 12,000 optimizer steps.}
    \label{fig:failed-conditioning}
\end{figure}

\subsection{Baseline versus implicit Patch Forcing}

My revised Patch Forcing model converged faster and reached a clearly better held-out error than the frame-level baseline. Figure~\ref{fig:quantitative-results} shows the two most informative evaluation curves. The left panel is teacher-forced one-step MSE, while the right panel is the 20-step autoregressive rollout metric. The magenta curve is my Patch Forcing model and the green curve is the frame-level baseline.

In teacher forcing, the baseline begins to overfit at around 40,000 optimizer steps: its evaluation error trends upward even though training continues. In contrast, the Patch Forcing curve remains substantially lower and continues to improve. I believe the changing spatial corruption patterns are important here. Each episode is presented under many more local noise configurations, so the model sees a broader set of reconstruction problems even though the underlying video data are unchanged. This appears to act as a strong regularizer and allows the model to continue learning instead of memorizing the training distribution.

The difference is even more important in the right panel. At rollout horizon 20, the Patch Forcing model remains considerably below the baseline throughout the later part of training. This metric includes feedback from the model's own generated states, so it is more sensitive to object disappearance, identity changes, and compounding temporal errors. The larger gap therefore suggests that Patch Forcing improves not only one-step reconstruction but also the stability of the learned dynamics.

I use these common-protocol evaluation curves as the main quantitative evidence. I do not directly interpret the difference in aggregate training loss as model quality because the baseline and Patch Forcing runs are trained on different corruption distributions.

\begin{figure}[H]
    \centering
    \resultfigure{0.99\linewidth}{images/evaluation_comparison.png}
    \caption{Held-out evaluation during training. Left: teacher-forced one-step DINO-feature MSE. Right: autoregressive rollout MSE averaged over the first 20 generated frames. Magenta denotes Patch Forcing and green denotes the frame-level baseline. Lower is better.}
    \label{fig:quantitative-results}
\end{figure}

\begin{table}[H]
\centering
\small
\begin{tabular}{lccccc}
\toprule
\textbf{Model} & \textbf{Teacher forcing} & $\mathbf{H=5}$ & $\mathbf{H=10}$ & $\mathbf{H=20}$ & $\mathbf{H=40}$ \\
\midrule
Frame-level baseline & TBD & TBD & TBD & TBD & TBD \\
Patch Forcing & TBD & TBD & TBD & TBD & TBD \\
\bottomrule
\end{tabular}
\caption{Held-out DINO-feature MSE for the selected baseline and Patch Forcing checkpoints. The values are placeholders to be replaced with the final evaluation results. Lower is better.}
\label{tab:results}
\end{table}

The qualitative difference was larger than I expected. In baseline rollouts, I observed manipulated objects disappearing, changing identity, or becoming detached from the interaction. My Patch Forcing model preserved objects much more consistently through time and represented interactions between the robot and scene more coherently. The physics in my generated rollouts was not always accurate, but the model more often maintained the basic fact that an object continued to exist and remained involved in the interaction. In Figure~\ref{fig:qualitative-results}, I compare one held-out episode with ground truth, baseline, and Patch Forcing frames aligned in time.

\begin{figure}[H]
    \centering
    \resultfigure{0.98\linewidth}{images/qualitative_rollout_comparison.pdf}
    \caption{Decoded held-out rollout. Suggested row order: ground truth, frame-level baseline, and Patch Forcing. The selected episode should highlight object persistence and interaction consistency rather than only image sharpness.}
    \label{fig:qualitative-results}
\end{figure}

I interpret these results as support for my original hypothesis that heterogeneous patch corruption encourages stronger spatial reasoning. My successful model learned from the \emph{pattern of corruption} while receiving only a coarse frame-level indication of overall difficulty. My likely explanation is that the transformer had to use semantic and spatial evidence to decide which patches were reliable, rather than treating each patch as an independent denoising problem. However, my failed concatenation experiment is not enough to conclude that patch-level conditioning is harmful in general. Other conditioning mechanisms, particularly adaptive normalization closer to the source paper, require further investigation.

\section{Limitations and Conclusion}

My main limitation is experimental scale. A full run required roughly two days on six H200 GPUs, so I could not repeat every configuration with multiple random seeds or perform systematic sweeps over truncation standard deviation, signal schedulers, embedding sizes, and difficulty-loss weights. Consequently, I observe a large and consistent difference on one held-out split, but I cannot yet separate every causal factor or provide uncertainty estimates across runs.

I also note two methodological caveats. First, I use decoded images only to visualize predicted DINO features, not as pixel-space metrics. Second, my final model includes the difficulty head with a small auxiliary loss. Although the main architectural distinction is patch-wise corruption without patch-wise conditioning, I should independently disable this head in a future ablation. I should likewise evaluate the adaptive sampler under a fixed number of function evaluations before making claims about generation speed.

Within these limits, I reached the central objective of the project. I integrated patch-level corruption into an existing large world model, diagnosed an unstable conditioning design, and found a simpler alternative that produced substantially better quantitative and qualitative rollouts than the frame-level baseline. My strongest result is improved object persistence and interaction consistency. For a 400M-parameter model trained only in a frozen semantic feature space, I find this strong evidence that the structure of the noising task can materially change which spatial and temporal relationships the model learns.

\begin{thebibliography}{9}

\bibitem{oquab2023dinov2}
M.~Oquab, T.~Darcet, T.~Moutakanni, et al.
``DINOv2: Learning Robust Visual Features without Supervision.''
arXiv:2304.07193, 2023.

\bibitem{chen2024diffusionforcing}
B.~Chen, D.~Marti Monso, Y.~Du, M.~Simchowitz, R.~Tedrake, and V.~Sitzmann.
``Diffusion Forcing: Next-token Prediction Meets Full-Sequence Diffusion.''
Advances in Neural Information Processing Systems 37, 2024.

\bibitem{schusterbauer2026patch}
J.~Schusterbauer, M.~Gui, Y.~Li, P.~Ma, F.~Krause, and B.~Ommer.
``Denoising, Fast and Slow: Difficulty-Aware Adaptive Sampling for Image Generation.''
arXiv:2604.19141, 2026.

\bibitem{walke2023bridgedatav2}
H.~R.~Walke, K.~Black, T.~Z.~Zhao, et al.
``BridgeData V2: A Dataset for Robot Learning at Scale.''
Conference on Robot Learning, 2023.

\end{thebibliography}

\end{document}
